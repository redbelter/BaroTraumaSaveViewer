# Barotrauma Save File Viewer
# GUI: PySide6
# Parser: Python standard library only

pyside6>=6.6.0

# Standard library modules used:
# - gzip (compression)
# - zlib (decompression)
# - xml.etree.ElementTree (XML parsing)
# - pathlib (path handling)
# - collections (data aggregation)
# - re (regex)
